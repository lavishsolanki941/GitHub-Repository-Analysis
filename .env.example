# Optional: increases GitHub API rate limit from 60/hr to 5000/hr.
# Create one at https://github.com/settings/tokens (no scopes needed for public repos).
GITHUB_TOKEN=

# Optional: enables AI insights and the "Ask about this repository" Q&A tab.
# Without this, the app still works fully, just without those two features.
ANTHROPIC_API_KEY=
